# Bootstrap steps — WIP, will be replaced by bootstrap.sh
# Last updated: 2026-03-22

# Install Homebrew
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Clone dotfiles
git clone git@github.com:jtibau/dotfiles.git ~/dotfiles

# Install packages
brew bundle --file=~/dotfiles/Brewfile  # TODO: create Brewfile

# Symlink configs  # TODO: automate with bootstrap.sh
# ln -s ~/dotfiles/.tmux.conf ~/.tmux.conf
# ln -s ~/dotfiles/.gitconfig ~/.gitconfig
# ln -s ~/dotfiles/.vimrc ~/.vimrc
# mkdir -p ~/.config/ghostty && ln -s ~/dotfiles/config/ghostty/config ~/.config/ghostty/config
